import 'package:flutter/material.dart';

class SortedContainer extends StatelessWidget {
  const SortedContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
